# EA-4E.83 Original Codex Metadata Qualification

Parent: 15d14586702de3e316879189c84a01c985d7d8f3.
Scope: test-owned metadata admission plugin, nine pure admission tests, this evidence. Production source unchanged.

Original BinaryTests test_real_requalified_binary_is_present_and_exact and test_real_complete_parser_contract_is_accepted_without_model pass without modifying their assertions. Test-only defaults are injected during each selected node to confine schema/spool/SQLite/home to disposable basetemp and enforce structural/instance schema qualification. Actual binary identity/default pins are retained and compared against an explicit immutable EA82 tuple. Synthetic lineage is not real delegated work.

Exactly three actual metadata attempts across two nodes: version once for the binary node; version then complete help-terminated parser for the parser node. Exact argv, shell false, capture/text, isolated env/cwd, no stdin pipe, unknown options and per-node budget are enforced. Missing inherited stdin is forced DEVNULL. Popen admission is enabled only within those two node calls and restored afterward; non-selected nodes retain fake-only restrictions. Owned returned child handles are checked/cleaned after calls; no unrelated PID operation. Parent tripwires unchanged. Native-child network audit/universal sandbox is not claimed.

Working fake gate: 160 passed, 2 metadata nodes explicitly deselected, 3 subtests passed, filesystem/process events zero. Nine admission cases cover valid command, unsafe options, task command and budget exhaustion. Working metadata gate: 2 passed, metadata attempts 3, tripwire events zero, no task output. An earlier successful metadata run before explicit immutable tuple was also 2/3; retained as preliminary evidence, not final source proof.

Commands use no:cacheprovider plus existing fake-only/filesystem/report-host plugins. Fake gate includes EA82 adapter/schema suites and dedicated admission file. Metadata gate adds tools.ea4e83_codex_metadata_guard and selects only the two original nodes. Staged and actual committed exports require both gates independently before push; companion Obsidian records actual results.

This qualifies the metadata tests under isolated harness configuration, not ambient live runtime or real receiver execution. Missing genuine OpenCode replay capture and broad regression partition remain open. Protected successor WIP excluded; no files deleted. No real task/model, activation, GPU or ComfyUI. Production readiness unproven.
