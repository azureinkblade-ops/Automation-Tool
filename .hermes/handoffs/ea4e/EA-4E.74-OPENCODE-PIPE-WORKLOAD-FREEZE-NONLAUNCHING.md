# EA-4E.74 - OpenCode Pipe Workload Freeze

Baseline: 687fab883069ea1818889dca3fa308a8c5ba5b43.

Scope: four new acceptance-owned files: tools/ea4e74_pipe_probe.py, tools/ea4e74_pipe_inventory.py, tests/hermes_core/test_ea4e74_pipe_inventory.py, this evidence. Original OpenCode production implementation and test assertions, guard sources and protected WIP are unchanged.

Inventory freezes exactly 15 original real-Python pipe nodes and their exact -c snippets or malformed fixture bytes. No original snippet is evaluated or executed. The non-launching admission requires the frozen interpreter identity and isolated cwd; modified nodes, commands, executable/helper inputs, flags, snippets or arguments are denied. Dynamic malformed.py is verified against exact canonical LF source, not treated as arbitrary executable authority. The fake-only stdin test is not a launch workload.

An enumerated helper implements the same stdout/stderr sizes and markers, zero output, malformed JSON text, and original 30/60-second sleep choices. It installs the existing child audit policy before workload logic. There is no eval/exec/runpy of incoming snippets or task text. Future process qualification must replace admitted original inputs with this fixed helper and case, never create a -c/-m exemption.

Canonical LF source pins: helper 3f55c41a2515f7a8a40f9c0d605e17b72278a0798b1454121ddd9bd4383880c8; inventory f70201f90b0af6b01c70e2c3a692e958c414f516e33eb0f8ff6eb4bfffa1ffeb.

Thirty-eight dedicated fake-only tests: 15 exact input admissions, six changed-input denials, 15 output/sleep equivalence cases, unknown-helper rejection and an AST source-binding check comparing every frozen input directly against the original test file. Parent policy installation and sleep/output are faked for equivalence tests; no child policy is installed in pytest.

Working gate: 147 passed, zero fake-only/filesystem violations. Repeat the full fake-only ladder against staged-only and committed exports before normal push. No child processes, installed OpenCode receiver, model, network, production activation, GPU or ComfyUI activity occurred.

Dependencies: original test_opencode_adapter.py source for AST binding; prior admission/interpreter pin and child containment helper; predecessor guard and test ladder. Staged/committed exports reconstruct these declared dependencies without protected successor WIP. All inventory/helper/tests/evidence are acceptance-owned, not production execution authority.

Next: separately bounded 15-node/15-attempt pipe qualification guard, preserving original stdin=DEVNULL, byte-oriented capture, reader threads and Windows process flags. Register original handles independently because stuck-reader tests deliberately replace the adapter's process reference. Prove overflow, reader finalization, cancellation and cleanup against original assertions. Actual pipe behavior remains UNQUALIFIED by this non-launching checkpoint.

Fresh complete successor regression and unavailable historical capture/runtime evidence remain open. Production readiness is not established.
